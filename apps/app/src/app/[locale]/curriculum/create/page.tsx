import { CurriculumGenerationForm } from "@/features/curriculum/components/CurriculumGenerationForm";

export default function CreateCurriculumPage() {
  return (
    <div className="min-h-screen bg-gradient-to-br from-blue-50 via-white to-indigo-50">
      <CurriculumGenerationForm />
    </div>
  );
}
