# ===========================================
# Curri.AI Production Environment Variables
# ===========================================

# ===========================================
# Database Configuration
# ===========================================
# Supabase Database URLs
DATABASE_URL="postgresql://postgres:[PASSWORD]@[PROJECT-REF].supabase.co:5432/postgres"
DIRECT_URL="postgresql://postgres:[PASSWORD]@[PROJECT-REF].supabase.co:5432/postgres"

# ===========================================
# Supabase Configuration
# ===========================================
# Public Supabase Configuration (Safe for client-side)
NEXT_PUBLIC_SUPABASE_URL="https://[PROJECT-REF].supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="[ANON_KEY]"

# Supabase Service Role Key (Server-side only - NEVER expose to client)
SUPABASE_SERVICE_ROLE_KEY="[SERVICE_ROLE_KEY]"

# Supabase Edge Functions URL
NEXT_PUBLIC_SUPABASE_FUNCTIONS_URL="https://[PROJECT-REF].supabase.co/functions/v1"

# ===========================================
# Clerk Authentication Configuration
# ===========================================
# Public Clerk Configuration (Safe for client-side)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY="pk_live_[PUBLISHABLE_KEY]"

# Clerk Secret Key (Server-side only)
CLERK_SECRET_KEY="sk_live_[SECRET_KEY]"

# Clerk Webhook Secret (for webhook verification)
CLERK_WEBHOOK_SECRET="whsec_[WEBHOOK_SECRET]"

# ===========================================
# OpenAI Configuration
# ===========================================
# OpenAI API Key (Server-side only)
OPENAI_API_KEY="sk-[OPENAI_API_KEY]"

# ===========================================
# Application Configuration
# ===========================================
# Next.js Environment
NODE_ENV="production"

# Application URL (for webhooks and redirects)
NEXT_PUBLIC_APP_URL="https://curriai.com"

# JWT Secret for token signing
JWT_SECRET="[RANDOM_32_CHAR_STRING]"

# ===========================================
# Monitoring & Analytics
# ===========================================
# Vercel Analytics (optional)
NEXT_PUBLIC_VERCEL_ANALYTICS_ID="[ANALYTICS_ID]"

# Sentry for error tracking (optional)
SENTRY_DSN="[SENTRY_DSN]"

# ===========================================
# Rate Limiting
# ===========================================
# Rate limiting configuration
RATE_LIMIT_MAX_REQUESTS="100"
RATE_LIMIT_WINDOW_MS="900000"  # 15 minutes

# ===========================================
# Feature Flags
# ===========================================
# Feature toggles for production
NEXT_PUBLIC_ENABLE_ANALYTICS="true"
NEXT_PUBLIC_ENABLE_DEBUG="false"
NEXT_PUBLIC_ENABLE_BETA_FEATURES="false"

# ===========================================
# Email Configuration (Future use)
# ===========================================
# Resend API Key for transactional emails
RESEND_API_KEY="[RESEND_API_KEY]"

# Email templates
FROM_EMAIL="noreply@curriai.com"

# ===========================================
# Security Configuration
# ===========================================
# CORS origins (comma-separated)
ALLOWED_ORIGINS="https://curriai.com,https://www.curriai.com"

# Session configuration
SESSION_SECRET="[RANDOM_32_CHAR_STRING]"

# ===========================================
# Deployment Notes
# ===========================================
# 1. Replace all [PLACEHOLDER] values with actual production values
# 2. Ensure all SECRET keys are marked as "Server-side only" in Vercel
# 3. Test all environment variables after deployment
# 4. Monitor logs for any missing environment variable errors
# 5. Keep this file secure and never commit actual secrets to git
